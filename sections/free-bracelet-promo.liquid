<div class="promo-banner">
  <div class="promo-banner__inner">
    <span class="promo-banner__icon" aria-hidden="true">🎁</span>
    <div class="promo-banner__content">
      <h2>{{ section.settings.heading }}</h2>
      <p>{{ section.settings.text }}</p>
    </div>
    {% if section.settings.badge_text != blank %}
      <span class="promo-banner__badge">{{ section.settings.badge_text }}</span>
    {% endif %}
  </div>
</div>

{% schema %}
{
  "name": "Promo pulsera de regalo",
  "settings": [
    { "type": "text", "id": "heading", "label": "Título", "default": "Pulsera de tela Zaentra de regalo" },
    { "type": "textarea", "id": "text", "label": "Texto", "default": "Por tiempo limitado, cada pedido incluye una pulsera de tela Zaentra totalmente gratis. Próximamente estará disponible por separado a 1€." },
    { "type": "text", "id": "badge_text", "label": "Texto de la insignia", "default": "GRATIS por ahora" }
  ],
  "presets": [{ "name": "Promo pulsera de regalo" }]
}
{% endschema %}
